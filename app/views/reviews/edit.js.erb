console.log("lalala edit button works!");

status = true;


$("#new-form").hide();
$("#update-form").show();

$("#update-form").html("<%= escape_javascript render :partial => 'products/update', :locals => {:review => @review} %> ");

$('html,body').animate({scrollTop:$('#update-form').offset().top}, 1000);

var reviewId = "<%= @review.id %>";

$('.single-review[data-review-id=' + reviewId + ']').hide();


